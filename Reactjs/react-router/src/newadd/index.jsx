import React from 'react';
import PropTypes from 'prop-types';
import { Outlet } from 'react-router-dom';

NewAdd.propTypes = {

};

function NewAdd(props) {
    return (
        <div>
            <h1>  day la trang them bai viet</h1>

        </div>
    );
}

export default NewAdd;