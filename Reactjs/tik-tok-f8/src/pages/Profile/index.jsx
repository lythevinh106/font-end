import React from 'react';
import PropTypes from 'prop-types';

Profile.propTypes = {

};

function Profile(props) {
    return (
        <div>
            profile
        </div>
    );
}

export default Profile;